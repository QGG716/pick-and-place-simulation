# 允许脱垛接触结束、未决复核与主验收续改

## 范围与版本

基线及 fetch 后远端为 `432dfda6c1b204fdcca126b96e22a996f44d35ea`，工作区起始干净，
服务器没有遗留规划或 Isaac 进程。新隔离根目录：
`/root/autodl-tmp/m710-stack-transition-20260918/`，沿用已有 CPU/Isaac 环境，未安装依赖。

核心修复 `ca8dd2c590eed088d579052b336d54ebb90943a3`；唯一机制复验及唯一整机世界使用
`6f19e16acb269dc7ceef4ff4fdc46861a1672619`。整机首步故障之后的局部修复为 `56711af`，
最终源码/测试为 `f0aa89098e2bce93c4cf47742438b77115a9672e`。
原运行目录 `repo/` 保持实跑源码；故障后修复仅在 `review_repo/` 做 CPU 验证和新包绑定。
两个版本均核对 108 个源码/配置文件，清单分别保留，不回写历史运行身份。

## 两项生产语义修复

1. 按物理步开始时的阶段、附着与自由空间锁存判断许可。属于具名箱对、证据有效且
   在原许可范围内的步内接触，允许以步后几何正常结束；不再把“负 separation/非零响应
   与步后达到 5.2 mm 同时存在”本身判成冲突。步后原生盒体距离仍由接触裁决和
   `ActualStackContactMonitor` 共同消费。
2. `first_conflict` 只保留历史首疑点。当前未决在 `active_issues`/`pending`，确证违规在
   `confirmed_violation`，复核结果保留于 `issue_history`。缺少有限接触点时保持；同一未中断
   接触过程补齐有效特征、核对保留的许可/几何条件后可转为 `EXPLAINED`，记录解决时间和依据。
   原记录和计数保留，不伪造 LOST。不能解释的来源或运动连续性问题仍保持并有界停止。
   无效测量、错误形状、超原范围及几何违规不能由后来分离、LOST 或时间流逝清除。

自由空间后的残留报告必须核对：源于已许可接触过程、持续事件未被 LOST/新 FOUND 截断、
可观测法向/face index 特征、前后实际盒体均满足 5 mm、连续 PhysX 读回及速度一致性，
以及法向投影变化能否解释接触点分离量。数值表示误差来自 float32 输入的 ULP，未增加
物理净空 epsilon 或冲量忽略阈值。接触偏移仍只用于报告特征来源核查，不膨胀实体。
此解释不授予实体接触许可，不将 free-space 锁存退回 false，也不强制保持 extraction 标签。

保留原始接触位置、法向、冲量、actor/collider 配对及步前/步后状态。可观测特征不是
虚构的 manifold ID；没有声称获得不可用的接触生成精确子时刻。原第 3027 步缺失的完整
位姿没有被邻近 5 fps 数据插值补造，旧试验仍为 REVIEW_REQUIRED。

保持期间继续物理监测，冻结轨迹推进，禁止额外抓取、释放和首次接管。复核解决后沿用
原指令时钟与 free-transit 边界门恢复，不跳过工艺点。

## 未改动的规则

5 mm 运行净空、5.2/5 mm 脱离阈值、20–50 mm 实际释放、25/35/45 mm 名义候选、
5 mm 高度储备和 3 mm 定向规划储备均不变。原允许接触范围、邻箱扰动、contactOffset /
restOffset、响应和求解器、机器人/工具/接收机保护、布局、箱体尺寸/质量、CAD、
IK/FK、关节/Jacobian/径向限制、PD、力矩、速度、插值与物理/控制频率不变。
未增加脱垛距离或抬高路线，未恢复 CPU 规划墙钟截止；真实驱动力矩遥测仍单列未评估。

## 定向验证

旧实现生产入口复现：相同水平滑出/竖直接触特征，步后 **5.19 mm 不保持**；
仅将步后间距改善至 **5.21 mm 就保持且不能锁存自由空间**。新实现两者均无冲突，
只有达到 5.21 mm 的输入锁存自由空间。使用的负分离量为 **−0.00035 mm = −0.35 μm**。
结果见 `old_transition.json`、`new_transition.json`。

最终一次组合回归：**135 passed in 17.11 s**，不是多次测试数相加。
八个定向模块：stack_clearance_semantics、isaac_contact_callback_index、
m710_runtime_contact_policy、zero_point_contact_resolver、poc_release_reserves、
ideal_release_handoff、poc_continuation_state、obb_query_identity。

覆盖正常接触结束、5.19/5.21 mm 单调改善、自由后 4 mm/相交拒绝、许可生命周期断开、
错误/非有限测量、世界/任务身份、补证恢复、未知到期停止、确证违规不清除、任务隔离、
实际回调接线、保持期间动作门及恢复后的实际时钟分支。原释放交接、step1.1 续箱、
接收顶面身份及 4.958109 mm transit 拒绝保留。没有跑全量 pytest、104/129 或性能扫描。

## 唯一短机制复验：PASS

两原生箱 0.6×0.4×0.3 m、42.5 kg，原接触参数、16/4 求解器迭代、240 Hz；
上箱真实静置 0.5 s 后按实际姿态建立有限力直线约束。初始有响应接触点 **388 个**。
驱动读回为 1000 N、20000 N/m、2000 Ns/m，所属刚体 `/Probe/target`、X 轴及实际锚点正确。
未调整驱动设置；只修正静置/附着阶段标签并补读回。运动中位姿重置 **0 次**。

- 新机制世界 `stack-slide` 身份保存在 `mechanism_summary.json`。
- 步 **3027**，物理时间 **12.6166667 s**，几何距离 **5.28164828081662 mm**，
  仍有负接触点分离量与非零冲量，按合法步内接触结束正常进入自由空间。
- 转换窗口保存完整前后状态。窗口内 5 条自由之后的报告经持续特征和几何核查解释，
  不是等冲量归零或 LOST 才通过。没有待解释冲突。
- 随后反向接近，步 **3113** 在实际间距 **4.923841598286571 mm** 触发
  `ACTUAL_FREE_TRANSIT_STACK_CLEARANCE_LOST`，不退回接触豁免。
- 物理时间 **12.975 s**；这次预期保护停机是机制验收的反向检查，机制结果为 **PASS**。

没有要求重现旧 0.066437 mm，也没有把新世界证据拼回旧机制缺失帧。
64 帧录像全部解码通过，640×360、5 fps、正常物理时间。

## 唯一整机新世界：首箱第 0 步停止，0/5

机制通过后启动世界 **1789719626.6563382**。固定初始 40 箱，无历史完成事件。
实际静置 **0.9041667 s** 后得到最高排集合：
`carton_l07_c00/c01/c02/c03/c04`，分母 5。
正常入口首个目标为 `carton_l07_c02`；没有强制设定后续成功顺序。

首箱 CPU 完整结果保持兼容，复用原 motion SHA
`ee3a667fd7da12f53e506d9818df1e26b6a58c58e8be154a744ed2d45862a49e`，本轮未从零搜索。
首箱/续箱配置事先配对验证，续箱仍使用现有充分有限配额。实跑前完成当前执行源码的
preflight、export、readback，三项通过并保存只读 first_feasible。故障后最终源码另生成新包，
同样三项通过；旧包未改标签。历史 1334.47 s 规划时间不计成本轮新增搜索时间。

**实际停止过程**：第 0 步、任务物理时间 **0.004166667 s**、轨迹时间 **0**，
零时长 `home=[0,0]` 位置标记抢占同时开始的 `pregrasp` 区间。尚未附着、尚未自由的
目标箱仍受 `carton_l06_c02` 支撑，实际间距 `5.550826011613275e-7 m`，当步没有接触回调。
统一几何裁决因错误阶段误套用自由净空规则，记录 `ACTUAL_FREE_TRANSIT_STACK_CLEARANCE_LOST`。
这不是已证实的物理碰撞、脱垛不可行或释放失败。

随后的空遥测数组汇总又抛出 `zero-size array ... maximum`，覆盖了顶层异常名；
真正首因仍保存在 `execution_events.json` 和完整步记录中。保留两层错误，没有改写原日志。
初始化实际 q 在 fixture 中单独标作步前最后读数；原运行没有保存该步后的 q，不补造。

| 目标 | 实际抓取/释放 | 理想接收/送出 | 撤离 | 结果 |
|---|---|---|---|---|
| c02 | 0 / 0 | 0 / 0 | 未执行 | 初始阶段接线误停 |
| c00、c01、c03、c04 | 未执行 | 未执行 | 未执行 | 前序阻塞，尚未生成本世界后续任务 |

新增实际物理接收 **0**，理想接收 **0**；第二箱原脱垛阻塞 **未验证到**。
没有进入续箱离线规划，未宣称实时在线完成。没有重开第二个整机世界，也未清理下层 35 箱。

## 实际故障的局部修复与交付边界

`resolve_actual_task_stage` 现在跳过零时长 home 位置标记，由真实 pregrasp 区间接管时间 0；
没有把 home 加入接触豁免，零时长 place/contact 的既有生命周期仍保留。
没有遥测样本时直接保留原停止原因，异常检查点新增当前机器人张量读回，避免二次汇总错误遮盖首因。

保存的首步实际箱对通过 `replay_initial_stage.py` 调用生产解析与裁决复查：
几何距离不变，阶段由错误 home 改为 pregrasp，不再误停，也未提前锁存自由空间。
该离线结果不把已结束的世界改成成功。局部修复通过上述最终 CPU 回归，**未再次启动 Isaac**。
当前唯一最值得做的下一项是使用已修正阶段接线的新执行包，再验证正常整机流程能否到达第二箱；
原第二箱物理问题仍无本轮整机结论。

精简证据：`docs/evidence/m710_poc_stack_contact_transition_followup/`。
服务器机制录像：`repo/outputs/stack_slide/slide.mp4`，SHA
`d4dd85a849b035fad79d69be100cc16f5785872002a8516c9ed558f21567341f2`。
整机停止早于首个视频采样，`repo/outputs/isaac_highest_row/replay.mp4` 仅为空容器、0 帧，
不能当作有效整机录像；没有为录像再开世界。
完整原始证据在该目录的 `stack_clearance_steps.json`、`failed_actual_carton_states.json`、
`initial_highest_row.json`、`execution_events.json`，大日志为任务根目录 `isaac_highest_row.log`。
最终源码执行包在 `review_repo/outputs/initial_plan/first_feasible/`，仅 CPU 交付，未物理执行。
