# M-710 动作质量与有限预算：2026-09-15

本轮从 `981a938ac1ebe02ce2671dfa8ac85da003250abd` 开始，初始工作区干净。原世界 `1789448839.7527404` 已结束：最高排实际接收 **4/5**，理想送出 **3**。保留[原报告](m710_ideal_outfeed_20260915.md)和执行源码归档，不用当前 HEAD 追认旧录像的版本。所有规划、数值检查和 Isaac 计算均在 GPU 服务器执行；本地仅编辑、传输和 Git 交付。

**本轮完成实现、同状态比较、第五箱有界复现、65 项检查和真实连续片段；第五箱完整路径仍失败。** 新世界仅完成首箱取放及第二箱抓取，未重复卸整排，不与旧世界累计箱数。

## 指令与实际运动定位

分析原第二箱确认释放至第三箱 `carton_l07_c03` 抓取的事件、240 Hz `joint_tracking.csv`、motion/bundle 和实际场景，没有重跑基线。安全撤离、空载自由连接、末端受控接触分别为 **1.266667、61.318312、4.598354 s**。

自由段指令累计运动 20.459575 rad，实测 20.497867 rad；指令反向次数 `[17,13,13,16,18,4]`，实测 `[19,15,13,16,18,4]`，阈值 0.02 rad。最大跟踪误差 **0.00298582 rad**。主要绕行已存在于指令路径，实测主要跟随；不能仅凭视频宣称 IK 跳解、腕部翻转或驱动失稳。末端接触、撤离最大误差分别为 0.00072551、0.00055376 rad。

## 同实际起点、同目标比较

输入原 `segment_002/actual_remaining_state.json` 和第三箱 motion。仅简化严格空载自由前缀，再复查完整轨迹、当前接收占用、释放飞行和撤离包络。

| 指标 | 原路径 | 简化并重验后的候选 |
|---|---:|---:|
| 自由段节点 | 59 | 11 |
| 六关节累计量 / rad | 20.468292 | 15.942225 |
| J4 / rad | 4.994674 | 3.394672 |
| J5 / rad | 2.141843 | 1.177702 |
| J6 / rad | 8.776300 | 8.728903 |
| 腕部合计 / rad | 15.912816 | 13.301277 |
| 反向次数，阈值 0.02 rad | 81 | 11 |
| 工具朝向 SO(3) 累计量 / rad | 8.927175 | 7.214287 |
| 自由段时间 / s | 61.318312，旧实测 | 17.488727，新计划，尚非实测 |

47 次捷径尝试、1369 次状态检查、22 次接受；15 s 后处理截止时保留完整验证的最佳路径。复用、简化及全部复查合计 **32.634560 s**；原第三箱从头完整规划 **395.040656 s**，工作量不同，不能当作从头规划提速倍数。另一个 60 s 限额的同状态从头接近探针在 57.006322 s（含预留）以 `STAGE_IK_DEADLINE/pregrasp` 失败，产物保留。

旧计时报告声称节点静止，实际执行源码却用分段线性参考，57 个内部节点不能称为 57 次已测停车。新执行使用解析五次参考，保留 9 个非共线内部拐角的停车。本例共线删除数为 0，收益主要来自几何简化；共线普通点非零速度通过由数值回归独立验证。J6 仍有约 8.73 rad 累计量，不能宣称全部扭转消除。最终 SO(3) 指标沿关节边以最大 0.04 rad 间隔采样，避免长边两端姿态相近时漏计整圈运动；早期运行归档保留原节点代理指标 8.924798/7.182232，不回写运行评分。

## 第五箱原状态复现

输入原第四箱 `segment_004/actual_remaining_state.json`，目标仍为 `carton_l07_c04`。请求预算 900 s，预留 15 s 给最终复查/导出。端到端约 **351.53 s**，规划内部计时 **351.169429 s**；实际覆盖 front/left/top 各 0/90/180/270°，12 个家族、3 轮、36 次姿态尝试。front 90°/270° 每轮各 4 条严格 IK，合计 24 条有效尝试记录，含跨轮重复，不是 24 个不同解。

48 次连接尝试、11821 次 RRT 迭代、30263 次状态验证；未进入提取或放置。**完整路径未恢复，未物理执行第五箱。** 卡在 pregrasp/approach，最终为候选墙钟切片耗尽；有限覆盖结束时未用满请求预算。保留逐分支碰撞和预算失败，不宣称物理不可达。原结果中的姿态上限标签仍为早期 12；最终代码已修正为三轮最多 36，原证据不回写。

## 实现与要求逐项对应

| 要求 | 落实情况 |
|---|---|
| 固定工程边界 | 布局、官方 FANUC、20 kg 工具、42.5 kg 箱体、官方有限关节限制、72 杯及三种 mask、原批准碰撞策略保留；运行门槛失败关闭 |
| 有界空载简化 | `bounded_shortcut` 先直连再确定性多尺度捷径；严格中间状态检查，共享截止、次数和状态预算；半条边不发布 |
| 工艺边界和绑定 | 仅无附着、无目标接触权限的自由段简化；direct 明确 free/contact 子边界；重映射 stage、事件、抓取和释放索引，重导出绑定 |
| 少量可行 IK 质量比较 | `_connect_pose` 比较最多两个可行组合；关节/腕部运动、反向、SO(3)、时间下界、关节裕量和 Jacobian 条件数作为有单位说明的软成本；碰撞和限位是硬门槛，无锁腕/角度取模 |
| 下一箱未知成本 | 返回 `None` 与 UNKNOWN/NOT_EVALUATED 语义；未计算的两任务代价为 `None`，不按零计分 |
| 有界真实前瞻 | 首目标最多两个接触候选，真实 IK、杯覆盖和接近路径；单次最多 4 s，请求合计 12 s；恢复杯 mask/接触上下文并清缓存 |
| 渐变朝向撤离 | 少量平移与最多 30° 渐变候选；逐姿态、飞行包络及驻留复查，最多比较三个安全撤离 |
| 顺序与同世界状态 | provider 保留实际剩余场景最高排/中心扩展顺序；前瞻仅建议，续箱仍用实测关节、箱体和接收状态重验，不覆盖实际状态 |
| 先广后深与预算 | 面/roll 家族轮转、20/50/100 s 候选切片，IK 分支分配剩余时间；连接 12 s、在线后处理 3 s，嵌套共享截止且有最终预留；第五箱有实际覆盖证据 |
| 失败分类 | 区分 IK 截止、连接失败、分支/候选/总请求截止和后续阶段失败；不将超时包装为 NO_IK/不可达 |
| 热点优化 | 同 q 复用 FK/几何位姿和工具 OBB，持久精确障碍物对象，移动时更新变换；保守 AABB 后仍执行原 Coal/SAT |
| 缓存正确性 | 键含障碍姿态/尺寸、目标、附着、杯 mask、阶段、策略、几何版本；同 q 移箱与对象改尺寸直接回归；显式 revision 刷新几何边界 |
| 几何与计时分开 | 删除同向共线普通点；工艺边界和非共线角保留停车，不引入未经碰撞复查的空间样条 |
| 解析参考与运行一致 | 同一五次 q/qd 用于采样/执行，解析速度、加速度、jerk 极值检查保留；重新绑定源节点时间、事件和 stage |
| 实际起始静止 | 真实位置驱动等待，同时检查 q 误差、原始 qd、相邻实测 q 的速度和稳定窗口；不覆写 measured |
| 历史复用范围 | 当前支持身份/姿态/尺寸、占用、短落箱和撤离完整重验，缺输入拒绝；成功后才绑定；非目标障碍微移和释放策略收紧均验证拒绝 |
| 力矩语义 | 显式输入 effort 不是隐式位置驱动输出；投影反力未经执行器力矩标定，逆动力学/重力补偿是模型估计；实际驱动输出仍 UNAVAILABLE，完整资格不虚报通过 |
| 录像与耗时 | 640×360、5 fps、1×物理时间；240 Hz 物理/控制与接收/碰撞监测保留，不把像素减少当作整机同比提速 |
| README 与历史 | 顶部为真实 4/5、3 送出和力矩不可用；旧 0/5/初始化结论归入历史，原报告与视频保留 |

相同 59 个状态的 validator 剖析均无碰撞失败：profile 总计 1.603→0.963 s，自碰撞 0.659→0.079 s，刚体/柔性工具生成各 118→59 次。详细计时有嵌套，不能相加为互斥总量。这不是完整 Isaac 提速倍数；旧第五箱约 891 s 的嵌套验证也不能全部称为 Coal 耗时。

## 检查与真实试验

GPU 的 `test_motion_quality`、`test_pinocchio_broadphase`、`test_layout_trajectory`、`test_timing`、`test_m710_replay_physics`、`test_adaptive_release_motion`：**65 passed，0.42 s**；执行脚本/桥接模块编译通过。包含旋转飞行、抛物线顶点、零飞行时长的包络包含检查与整圈旋转不漏计检查。未跑全量 pytest、104/129、40 箱清空或性能扫描。

新试验均单独标识：第一次因 PhysX 原始 qd 残差未过启动检查，保留 2 s 视频；实测位置两秒几乎不动，改为同时约束原始 qd ≤0.03 rad/s、位置差分速度 ≤0.001 rad/s、稳定 0.1 s，未改驱动增益或覆写状态。第二次真实抓取后，181 次释放预测均因箱底高差约 0.24–0.26 mm 超过 0.20 mm 门槛失败；视频、实际帧、失败画面和箱体状态保留。该异常没有完整 `result.json`/240 Hz CSV，不能冒称齐全；源码已将释放等待超时改为受控失败以输出完整结果和日志。

第三次给释放端加入世界 X 方向 -0.0003 rad 的候选，最大关节变化 0.000253482 rad。重新求解 FK 并复查整个轨迹、释放飞行及撤离，预测箱底高差 0.169707 mm，门槛不变。新世界 `1789467788.5108013` 首箱真实取放通过：物理 90.495833 s、Isaac 执行墙钟 874.656650 s；完整资格仅因实际驱动输出力矩不可用而未通过。

续箱默认搜索在首选 `carton_l07_c01` 的默认接触候选上失败，并按原顺序尝试后续候选。另把首选箱历史路径作为候选，从实测 q、当前箱体重新计算接触/附着及释放端；原接触点和 +Y 方向 3/6/12 mm 候选均被 `tool_rigid_118`—`carton_l07_c00` 碰撞拒绝。沿接触法向向外 **0.5 mm** 的候选仍满足原 2 mm 接触距离、杯覆盖和全部碰撞规则，完整路径/飞行/撤离复查 **16.867606 s** 通过；默认搜索在 456.276100 s 进度处停止，明确为被已验证候选替代，并非报告搜索完成或不可达。所有适配脚本、候选和失败均保留。

同世界执行第二箱：**7.270833 s** 时实际接触通过并附着，40 杯；7.8 s 按最小片段边界停止，Isaac 墙钟 **22.055201 s**。从首箱确认解除附着到第二箱实际抓取为 **8.545833 s**。片段内附着完好，最大附着位置误差 0.000015134 m；完整运输附着资格为 NOT_EVALUATED，不能将其当作第二次取放完成。碰撞范围、意外接触、关节跟踪、位置限位等运行检查通过，无 runtime stop；完整 schedule、第二箱释放/接收及其完整资格未完成。实际接收 1 箱，理想送出 0 箱，第二箱仅抓取。

本次第二箱自由段在当前状态下由 19 节点简化到 4 节点，保留 2 个内部拐角停车；自由段 5.341480 s。指令/实测六关节累计量 5.089312/5.095144 rad，腕部 3.985526/3.985391 rad，阈值反向均为 `[1,1,0,1,1,1]`，最大跟踪误差 0.001915485 rad。它是第一箱至第二箱的新世界实测，不冒充上述原第二箱至第三箱的同状态比较。

交付短片 10.0 s、50 帧；完整本次世界录像 98.2 s、491 帧。两者均逐帧解码通过、640×360、5 fps，全部为原始实际帧，无插帧或加速；同世界分段之间仅排除离线规划墙钟暂停，物理时间按 0.2 s 采样量化。`media_audit.json` 保存逐帧源文件与物理时间映射。

## 产物与交付

GPU 根目录 `/root/autodl-tmp/m710-motion-quality-20260915`：

- `baseline/`、`optimized-profile/`：事件对齐指标与定向剖析。
- `runtime-repo/outputs/same_state_003/`：简化 motion/preflight/bundle 和完整当前复查；`approach_comparison.json` 为失败的从头探针。
- `repo/outputs/fifth_reproduction/`：第五箱实际输入、逐候选记录、失败 motion。
- `isaac/`、`trial02/isaac/`、`trial03/isaac/`：各新世界真实结果；执行源码独立归档，不追认、不跨世界累加。

本地交付目录 `outputs/m710_motion_quality_20260915/final/`：`release_to_next_grasp.mp4` 为短片，`continuous_new_world.mp4` 为完整新世界录像，`evidence.tar.gz` 保存原始日志、结果、失败和运行源码，`round_summary.json`/`media_audit.json`/`delivery_manifest.json` 保存指标、帧映射与哈希。精简机器证据见 [evidence/m710_motion_quality_20260915.json](evidence/m710_motion_quality_20260915.json)。最终源码的 SO(3) 诊断采样改进晚于物理运行，已作直接回归；物理执行实际版本以各源码归档为准。完整最高排仍为后续目标，本轮已停止追加物理测试。
