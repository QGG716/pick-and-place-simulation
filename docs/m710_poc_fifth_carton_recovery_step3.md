# POC step 3：原第五箱恢复与阻塞定位

`carton_l07_c04` 已从原始实际起点生成完整 CPU 任务；独立 preflight、官方 export 和 bundle readback 全部通过，首次完整结果已只读保存。对应的唯一一次 Isaac 试验抓取成功，随后在搬运末段因箱体/接收机净空不足停止，未释放或完成。CPU 完整路径不等于物理完整循环。

## 身份与原始输入

Fetch 确认初始本地/远端均为 `a8af775b14afc6732c1b950004f9d2f3b900db34`，工作区干净、无原规划/Isaac 进程。后续 fetch 未发现远端新增提交。实现提交：

- 诊断、合法目标入口：`a8894fac6e9ae60de4b317da24b388843cd41acd`。
- 几何不变性修复及最终 CPU 搜索：`6dfa496e84ee5a8c469cc1c7f6568f35f99a71bc`。
- 存档初始化及执行绑定：`ef2dea2f053ec8291ab84752c43b093bd45658c2`。35 项 CPU 文件与上一提交一致；22 项执行文件及资产绑定脚本逐项核对。后续报告提交不改变实现。

沿用服务器既有环境，未安装依赖。V1/V2/V3/V4 分别隔离，未覆盖运行中的源码，保留原报告及物理证据。

原始状态：`/root/autodl-tmp/m710-ideal-outfeed-20260915-retry02/delivery/segment_004_actual_remaining_state.json`

SHA-256：`a7063b72dccf396e6a5ccaa24ffd2e1f12f599f7b7773153e081e95a7988f72e`。

它与同次 Isaac segment_004 状态字节一致，与 `ideal_plan_005/actual_scene_snapshot.json` 的全部 37 个活动箱体和实际 q 一致；旧 `fifth_reproduction/motion.json` 也绑定该快照与起点。[来源及各文件 SHA](evidence/m710_poc_fifth_carton_recovery_step3/input_provenance.json) 已保存。未改写原始文件或继承旧 PASS。

旧世界 `1789448839.7527404`，存档时间约 452.7625 s；不是存活的旧世界。实际起点（rad）：

```text
[-0.8773534893989563, 0.4496802091598511, -0.02197902463376522,
 0.0000014869361848468543, -1.0991413593292236, 4.018947601318359]
```

历史实际接收 c00/c01/c02/c03，理想接收 0，已送出 c01/c02/c03。c00 保留接收区姿态和“实际接收后理想送出”的来源。正常最高未完成行仅有 c04 合法；`--target-id` 只能选择正常候选，不能绕过身份、支撑和可抓条件。

## 已证实的错误

生产搜索出现 `VALIDATION_CONTEXT_CHANGED`：V2 分支已通过局部搬运并进入放置/撤离检查，却因冻结几何被修改而拒绝。真实 `_connect_pose` 复现定位到 `OBB.signed_distance_obb()` 对 NumPy 列视图原地归一化。例如存档 l03c00 的 rotation[0,0] 从 `0.9999999999955079` 变成 `0.999999999995508`。文件未变，内存输入被查询改写。

修复仅改为归一化临时向量，保留上下文校验、几何和容差。五项最小回归在旧实现全部失败，修复后通过。诊断在 boolean 转换前保存真实拒绝信息；内部 RRT 与成功 RRT 的完整边复检分开计数，按阶段/原因/对象对保留最多三个样本，共享上下文指纹。q、边/采样位置、候选/重试/种子、mask、目标/附着/接触许可和实现身份均可追溯；数值失败、耗尽、取消及异常分别保留。

## 实际搜索与完整交付

下列旧搜索各实际访问一个接触候选，因已确认的输入修改错误而取消，绝非墙钟截止或物理无解结论。

| 搜索 | 完成的 RRT 尝试 | 实际 RRT 迭代 | 取消前进展 |
|---|---:|---:|---|
| 002 | 55 | 28097 | 接近后进入搬运/替代放置 |
| 003 expanded | 16 | 13688 | 接近后进入搬运/替代放置 |
| 005 local720 | 6 | 9306 | 局部搬运成功，撤离上下文拒绝 |
| 006 deep | 4 | 5979 | RRT 找到连接，接近上下文拒绝 |

最终 007 使用原始状态，显式有限配额：续箱连接 **54000**、局部 Cartesian **720**、请求 IK seed **175799**。分支默认值仍为首箱 600、续箱 1800、局部 240；无业务墙钟或外层 timeout。

- 生成 108 个候选，**实际搜索 1 个**，重试 0，其余 107 个未搜索；找到首条完整任务即停止。
- RRT 迭代 1667，Cartesian 样本 208，完整边状态采样 104570（含重复端点）。
- 规划 **2864.223 s（47.7 min）**；路径 250 个节点，纵向接收机，`IDEAL_RECEPTION_RELEASE`，命令时长 **178.139 s**。
- 接近/接触、脱垛、搬运、放置、释放、撤离及末端停留均纳入完整结果，未做腕部或节拍优化。

V3 保存 PASS motion 后，preflight 因部署副本中 `scripts/analyze_gripper_step.py` 的 LF/CRLF 差异停止。V4 恢复该文件的原始 Git 字节，未改资产清单，对**同一 motion**独立 preflight、export、readback 全部通过；CPU 和场景指纹一致。原失败日志保留。

只读首个完整结果：

```text
/root/autodl-tmp/m710-poc-fifth-step3-v4-20260917/outputs/fifth_search_007_delivery/first_feasible/
```

motion SHA：`52ac4db8a2987bcc1f9d4898dffe8c3e6643f6baea68eda750e8d9257ba302f1`。

[实际搜索统计](evidence/m710_poc_fifth_carton_recovery_step3/final_cpu_search_summary.json) 与 [独立交付状态及各文件 SHA](evidence/m710_poc_fifth_carton_recovery_step3/cpu_delivery_status.json) 已保留。交付状态中的 `isaac_executed=false` 是启动物理试验前的 CPU 交付记录，物理结果另存，不回写该记录。

## 代表性拒绝复查

修复后 8 个接近样本和 1 个搬运样本均经真实生产验证器重放一致。旧成功 RRT 的完整边复检拒绝也重放一致，没有被提升为合法路径。当前直接搬运边首个拒绝点：

```text
q = [0.9054744713331971, -0.7207916993920802, -0.5001000298523148,
     1.3188756037094822, -0.9448234795311973, 1.8972770876073515]
pair = carton_l07_c04 / carton_l06_c04
distance = 0.0049968880257103 m; required = 0.005 m
classification = CLEARANCE_INSUFFICIENT; intersection = false
```

Jacobian condition 7.4526，关节裕量全部为正。这是首个拒绝点，不是整条边最小净空；生产搜索继续现有外移路线并最终成功。

原 STEP 来源网格复查同时发现真实接触、净空不足和保守代理差异：旧接近样本中工具第 0 刚性零件对 l06c01 的代理距离 4.999394 mm，原网格距离 11.137361 mm。网格线性弦高 0.05 mm，不是解析 BRep 证明；单一对象对通过也不是完整构型通过。因此未缩小/删除代理或降低净空。完整 q、边、对象几何和来源见 [可重放精简证据](evidence/m710_poc_fifth_carton_recovery_step3/README.md)。

## 单次物理验证

新世界 `1789650082.7890007` 明确为“按存档重建的新世界工况”，历史事件仅作上下文。保留活动 c00 的既有理想送出身份、所有活动箱体及原观察速度。历史运输在初始化/静置期间保持，之后以存档时间作为事件时钟偏移继续。

静置 0.904167 s 后，37 个活动箱体及实际 q 的既有绑定检查通过，无需重新规划。初始化接线也支持在绑定失效时暂停同一新世界、等待合法离线重规划；不重置或替换运行中的机器人/箱体。

640×360、5 fps、正常物理时间，240 Hz 物理及既有控制/碰撞监测频率不变，仅执行 c04。实际接触检查 PASS，48 个杯实际接触，47.408333 s 抓取成功，73.020833 s 通过实际自由搬运门槛。**164.879167 s 停止**，共 39571 个物理步；没有追加试验。历史 c00 在此世界继续送出，不计为第五箱完成。

本轮 c04 新增计数：**实际抓取 1、实际释放 0、实际接收 0、理想接收 0、理想送出 0、工作流完成 0**。历史实际接收 4 保持原始来源。停止时仍附着，实际状态经正常 `apply_actual_motion_state` 入口被明确拒绝为下一任务起点：`between-task scene update requires actual attachment release`；未补造释放或接收事件。

首个物理拒绝（完整记录及关节/姿态见 [物理试验摘要](evidence/m710_poc_fifth_carton_recovery_step3/physical_trial_summary.json)）：

```text
stage = transit; attached = true; actual_free_space = true
q = [-0.7828850150108337, 0.27441492676734924, -0.30381056666374207,
     -0.0004958999925293028, -0.9924537539482117, 3.924762487411499]
pair = /Validation/Scene/carton_l07_c04
       /Validation/Scene/conveyor_longitudinal/TopCollision
PhysX separation = 0.0049581085331737995 m; required = 0.005 m
classification = CLEARANCE_INSUFFICIENT; contact_permission_source = null
```

不足约 **0.041891 mm**，不是已确认的实体穿透。通用停止名 `UNEXPECTED_ROBOT_OR_RIGID_TOOL_PROXIMITY` 在此对应上述箱体/接收机对，并非已证实的机器人/刚性工具碰撞。仍处于 transit，尚未进入允许接收支撑接触的 place 阶段。离线调用同一生产分类器完全重现该拒绝；最终保存箱体姿态的 OBB/接收机距离为 **4.905988 mm**，也未达到 5 mm。此重建数值与 PhysX 接触点记录使用不同几何/数值表示，不冒充完全相同的距离查询。

以 CPU 原名义附着关系作同构型对照：实际 q 的箱体/接收机距离 **5.155768 mm**，指令 q 为 **5.804531 mm**。因此已确认的是实际运行在放置前净空不足；名义附着、实际附着与跟踪偏差的贡献还不能仅凭这一对照完全归因。未修改净空、接触许可、工具代理、PD 或其他物理门槛。

所有关节均在官方范围内，该构型 Jacobian condition **4.710756**。全程最大关节跟踪误差 **0.008668 rad**（现有上限 0.05 rad）。停止前附着始终完整，峰值附着位置/旋转误差约 **0.015184 mm / 0.000042323 rad**。结果中附着全程资格为 `NOT_EVALUATED`，原因是完整运输未跑完，不是检测到了脱落；释放/放置相关条目也未完成，不能据其汇总失败列表另行认定释放适配器已在运行中失效。真实驱动输出力矩不可用，单独为 `NOT_EVALUATED`，不是这次停止原因。

本次目录：`/root/autodl-tmp/m710-poc-fifth-step3-v4-20260917/outputs/isaac_fifth_archive_once/`。视频为其中 `replay.mp4`，完整解码 **824 帧 / 164.8 s**；SHA-256：`326b5fc3bf442cd827454bca6492c60bc43bed1aec9c00346de95621772f6097`。大日志、视频、原始状态留在服务器。原录像不作为本次证据。

## 测试与保留范围

- CPU 几何、诊断确定性、真实目标/预算入口、RRT 内部/完整边复检及续箱状态：**70 passed，4.44 s**。
- 存档来源/身份、静置后实际状态载入、执行包绑定及相关流程：**53 passed，2.63 s**（续箱回归部分重合）。
- 一个旧导出测试夹具隐式启动首箱规划，发现后已取消，未计为导出验证或交付；实际验证采用第五箱完整结果。

未跑全量 pytest、104/129、整排或性能扫描。保留 POC 理想吸附/接收/送出、step 1.1、完整结果优先、有限新种子、5 mm 外部对象对净空、自碰零附加净空、有限墙体及既有接触许可。布局、安装、箱体/CAD、IK/FK、Jacobian、关节/径向门槛、质量/惯量、PD/力矩、速度比例及插值未改。真实驱动力矩遥测缺失单独记录，不作为 POC 前置阻塞。

下一步唯一重点：针对本次接收前固定构型，核对实际附着变换及跟踪误差如何消耗名义净空，并使接收前路径在真实执行中保持现有 5 mm 要求；不先降低门槛或提前授予 place 接触许可。
