# 自动重开并继续剩余三箱

2026-09-20，用户在获知原进程已消失后明确授权“自动重开”。本任务后续如需因
运行中断重新建世界，不重复请求相同授权；每个世界仍独立记录，不合并完成分母，
不改写物理失败，不新增碰撞豁免。

本轮从世界 `1789726502.8089688` 的第二箱完成快照继续。快照未附着，保留 38 箱，
SHA-256 为 `8030225e305d611bb3d832abafd63d8ef43bcfb8c619ab6fda6ac4e8b8b9136c`。
历史已处理 c01/c02（完整前缀 `carton_l07_`）仅是初始化上下文；本次范围为
c00/c03/c04，分母 3。此前世界的 1/5 与 1/4 分别保留。

源码以已推送提交 `ab3c63d07e1bfbd3be58ab7451903f1ade35fb1e` 为基础；服务器隔离目录：
`/root/autodl-tmp/m710-auto-resume-20260920/`。复用现有 CPU/Isaac 环境和模型资源。
此前 c03 规划没有产生完整 motion 或可恢复搜索树，故从同一实际快照重新运行现有
规划器；不把中断的局部连接当成完整方案。使用现有观察性 diagnostics 保留搜索记录，
不改变候选、有限迭代、净空、控制、动力学或无业务墙钟截止的配置。

独立后台执行入口依次完成首个完整 CPU 方案、preflight/export/readback，再自动启动
新世界并用实际末态续箱。保留 640×360、5 fps、正常物理时间。第二箱专属豁免目标
c01 不在本次剩余范围内，因此本轮不启用该运行参数。

首个完整 CPU 方案耗时 7896.27518286556 秒，目标 c03。预检、导出、读回均通过。
新世界 `1789875150.9359868` 已启动，初始实际最高排范围为 c00/c03/c04，分母 3。
初始化画面已下载至 `outputs/delivery/20260920_ab3c63d/`，6 个文件完成本地字节数和
SHA-256 校验，PNG 可打开；录制中的 MP4 尚不作为已交付视频。

用户随后要求后两箱简化碰撞检测。当前 c03 与整个物理世界继续使用原运行源码，
仅等待中的续箱调度器暂停以接入独立 `planner_repo`。c00/c04 的 CPU 查询改为批量
15 轴 SAT、保守包围盒面轴筛选，以及基于上次精确距离的刚体位移界复用。
复用下界为原距离减去两物体的平移与旋转最大位移；下界不够或证据未知时恢复
原 Coal 查询。累计位移始终从最后一次精确查询计算，不从缓存命中重新起算。
几何变更代数、对象尺寸和对象对绑定缓存；不减小间隙、不漏掉近邻、不改变密集
路径采样、有限求解额度或 Isaac 保护。CPU 源码身份与原运行源码分别记录。

服务器定向测试 45 项通过。固定种子的 432 个实际路径采样点新旧拒绝结果完全一致；
状态检查耗时从 5.4442469254 秒降至 4.5518936068 秒（约缩短 16.4%）。这是有限样本的
检查计时，不代表整箱规划耗时承诺；SAT 独立算子约快 9 倍，也不等于全流程快 9 倍。
23 个执行实现文件与活跃世界逐字节相同，候选输出仍须通过正常预检、导出、读回和
活跃世界源码绑定核验，不能改写或删除原指纹。

实际逐箱结果将在产生后补全；目前尚不计新世界完成数。
